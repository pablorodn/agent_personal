{
  "name": "agent_total-js-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Verificacion puntual de JS inline (chat.html) con DOM real via jsdom. No es un toolchain de build; el proyecto sigue siendo Python/uv.",
  "scripts": {
    "test:js": "node tests/js/test_submit_chat_formdata.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
